@model PeopLost.Web.Models.ListAlertViewModel

@{
    ViewBag.Title = "List";
}

<div class="container">
    <div class="col-lg-12">
        <div>
            <h2>Liste des alertes<span class="glyphicon glyphicon-option-vertical"></span></h2>
            
        </div>
        @*@Ajax.BeginForm("Search", new AjaxOptions() { HttpMethod="Get", InsertionMode= InsertionMode. }){
            <span><input type="search" placeholder="Tape the name here" id="tsearch" />
                <input type="button" id="btsearch" value="search" /> </span>
        }*@
        @foreach (var item in Model.Items)
        {
            <div>
                @Html.Partial("_Person",item)
            </div>
        }
    </div>
    
</div>
@Scripts.Render("~/dist/js")
